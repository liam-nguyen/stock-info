# Include optional dependencies for local development
# This installs @sparticuz/chromium (~120MB) to prevent Next.js build errors
# However, we use local Chrome (via PUPPETEER_EXECUTABLE_PATH) so chromium binary is not used
# The package needs to be present for Next.js module resolution but won't actually run

